class Covert_number:
    def convert (updateCount): 
        print(updateCount + 1 , "SSSSSSSSSSSSSSS")
        # splited = updateCount.split('_')
        # num = int(splited[1]) + 1
        # num = str(num)
        # if (len(num) == 1):
        #     num = '0' + num
        # formedWord = splited[0] + '_' + num
        # count = formedWord
        # print('formedWord >>', formedWord)
        return updateCount + 1